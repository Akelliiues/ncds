# Git Auto Commit and Push Rule

- **Rule**: Whenever any feature, bug fix, security enhancement, or UI modification is completed, automatically stage (`git add`), commit (`git commit -m "..."`), and push (`git push origin main`) to GitHub immediately without needing to ask the user for explicit confirmation.
